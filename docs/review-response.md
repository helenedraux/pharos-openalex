# Review disposition: coverage report and exports

The pasted review usefully highlights readable exports, provenance, early definitions, coverage context, and duplicate saved entries. Implemented: PDF/Excel/CSV alongside JSON, common export snapshot IDs, table denominators, early coverage strip and definitions, preserved raw-category exports, clearer report action text, and deduplication of the saved sidebar without deleting data. The PDF uses a labelled annual line chart and does not depend on browser print settings.

The reviewer inferred a report-reproduction/comparison goal that is not Pharos's agreed product boundary. Cross-source reproduction, benchmarking, citation/impact reporting, OA, and generated narrative were not added. Affiliation coverage remains visible because the user explicitly requested it. Institutional relationships are not guessed from names; shared-work results disclose that affiliated versus external organizations have not been independently classified.

Possible later improvements within scope include other entry routes, deliberate saved-snapshot navigation, expandable small work types, temporal composition views, and institution-relationship metadata from authoritative assertions. These are not required for the export increment and are not represented as implemented.
